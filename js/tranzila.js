// ============================================================================
// אינטגרציית סליקה עם Tranzila.
// הקישור בפועל (CONFIG.TRANZILA_TERMINAL_URL) הוא עמוד "חנות וירטואלית" של טרנזילה,
// ולא Terminal API - הוא אינו תומך בהעברת סכום דרך פרמטרים ב-URL (נבדק בפועל).
// לכן הכפתור פשוט פותח את הקישור כמו שהוא, בלי לצרף אליו כלום - בדיוק כמו לחיצה על הקישור.
// פרטי הכרטיס המלאים מוזנים אך ורק בדף של Tranzila - לעולם לא ב-Sheets/בשרת שלנו.
//
// מאחר שהעמוד הזה לא מחזיר אותנו לאפליקציה עם תוצאה, ההוכחה היחידה לסליקה מוצלחת
// היא שהמשווקת חוזרת בעצמה לאפליקציה (לפי ההחלטה המקורית שלא לממש אימות בצד שרת).
// הטיפול בזה נמצא ב-app.js: finalizePendingTranzilaIfAny().
// ============================================================================

/** שולח את הדפדפן לעמוד הסליקה של Tranzila - בדיוק כמו לחיצה על הקישור */
function goToTranzilaTerminal() {
  window.location.href = CONFIG.TRANZILA_TERMINAL_URL;
}

const TRANZILA = { goToTranzilaTerminal };
